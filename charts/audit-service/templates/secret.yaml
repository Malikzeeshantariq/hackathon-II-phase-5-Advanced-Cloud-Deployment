# T025: Kubernetes Secret for audit-service

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "audit-service.fullname" . }}-secret
  labels:
    {{- include "audit-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: {{ .Values.env.DATABASE_URL | quote }}
