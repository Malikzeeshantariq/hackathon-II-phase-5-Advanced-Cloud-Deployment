# T014: OKE Free Tier cloud overrides for audit-service
# Spec Reference: contracts/helm-values-schema.md — values-cloud.yaml

image:
  repository: ${OCI_REGION}.ocir.io/${OCIR_NAMESPACE}/audit-service
  tag: ${IMAGE_TAG}
  pullPolicy: Always

resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    cpu: 150m
    memory: 192Mi

dapr:
  sidecar:
    cpuLimit: "50m"
    memoryLimit: "96Mi"
