# T4-06: Backend Kubernetes ConfigMap
# Spec Reference: spec.md FR-5.1, data-model.md Backend Resources
# Plan Reference: plan.md Section 9

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "todo-backend.fullname" . }}-config
  labels:
    {{- include "todo-backend.labels" . | nindent 4 }}
data:
  cors-origins: {{ .Values.env.CORS_ORIGINS | quote }}
