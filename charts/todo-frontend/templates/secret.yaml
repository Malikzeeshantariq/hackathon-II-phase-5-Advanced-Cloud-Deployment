# T4-07: Frontend Kubernetes Secret
# Spec Reference: spec.md FR-5.2, data-model.md Frontend Resources
# Plan Reference: plan.md Section 9

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "todo-frontend.fullname" . }}-secret
  labels:
    {{- include "todo-frontend.labels" . | nindent 4 }}
type: Opaque
stringData:
  better-auth-secret: {{ .Values.env.BETTER_AUTH_SECRET | quote }}
  database-url: {{ .Values.env.DATABASE_URL | quote }}
