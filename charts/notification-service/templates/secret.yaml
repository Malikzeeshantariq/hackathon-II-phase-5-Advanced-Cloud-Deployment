# T025: Kubernetes Secret for notification-service

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "notification-service.fullname" . }}-secret
  labels:
    {{- include "notification-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: {{ .Values.env.DATABASE_URL | quote }}
