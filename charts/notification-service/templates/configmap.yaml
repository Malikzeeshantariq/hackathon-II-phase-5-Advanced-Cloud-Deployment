# T025: Kubernetes ConfigMap for notification-service

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "notification-service.fullname" . }}-config
  labels:
    {{- include "notification-service.labels" . | nindent 4 }}
data:
  # Add non-sensitive configuration here if needed
  app-id: {{ .Values.dapr.appId | quote }}
