# T025: Kubernetes Secret for recurring-service

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "recurring-service.fullname" . }}-secret
  labels:
    {{- include "recurring-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-url: {{ .Values.env.DATABASE_URL | quote }}
